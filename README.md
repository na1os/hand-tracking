Hand Tracking Mouse Controller (Python)

A simple hand tracking application that uses a webcam to control the mouse cursor using hand movements.

Features
Real-time hand tracking using webcam
Cursor control based on index finger position
Optional click gesture (pinch)
Smooth movement

Requirements
pip install opencv-python mediapipe pyautogui numpy
Run (Python)
python main.py
